/**
 * divide function
 *
 * takes two values, divides first by the second, and returns the result
 *
 * @param a number
 * @param b number
 *
 * @returns number
 */
const divide = function(a, b) {
  return a / b
}

export default divide
