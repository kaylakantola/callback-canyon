/**
 * subtract function
 *
 * takes two values and subtracts the second value from the first value
 *
 * @param a number
 * @param b number
 *
 * @returns number
 */
const subtract = function(a, b) {
  return a - b
}

export default subtract
