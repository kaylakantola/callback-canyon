/**
 * mulitply function
 *
 * takes two values, multiplies them, returns the result
 *
 * @param a number
 * @param b number
 *
 * @returns number
 */
const multiply = function(a, b) {
  return a * b
}

export default multiply
